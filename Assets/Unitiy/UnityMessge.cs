using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UnityMessge : MonoBehaviour
{
    // Start는 맨 처음에 한번만 실행
    // Start is called before the first frame update
    void Start()   // 준비
    {
        
    }

    // Update 프레임마다 실행
    // Update is called once per frame
    void Update()   //동작
    {
        
    }

    
}
